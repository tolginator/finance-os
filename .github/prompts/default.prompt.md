You are an assistant helping build a personal investment intelligence OS.

- The MCP server is TypeScript; agents and quant tools are Python.
- Follow strict TypeScript and Python type-hinting conventions.
- All monetary arithmetic uses decimal types, never floating point.
- Gracefully handle malformed data — never crash on bad input.
- Every code change must include tests.
- Follow the existing code style found in each component's directory.
