{
  "name": "finance-os",
  "version": "0.1.0",
  "private": true,
  "description": "Personal Investment Intelligence OS",
  "type": "module",
  "workspaces": [
    "mcp-server",
    "web-ui"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "preflight": "npm run build && npm test && npm run lint && bash -c 'cd agents && if [ -d .venv ]; then source .venv/bin/activate && ruff check src/ tests/ && python3 -m pytest -m \"not integration\"; else echo \"⚠ Python preflight skipped (venv not set up). CI will catch Python issues.\"; fi'"
  },
  "license": "MIT"
}
